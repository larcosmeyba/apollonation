<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-sdk tools:overrideLibrary="androidx.health.connect.client" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Apollo Reborn"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name="com.apollonation.app.MainActivity"
            android:label="Apollo Reborn"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->
    <!-- Network access -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Camera for meal & progress photos. Runtime-requested at point of use. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!-- Photo selection uses Android's system photo picker (no permission required on Android 13+) -->

    <!-- Push notifications on Android 13+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Google Play Billing (RevenueCat) -->
    <uses-permission android:name="com.android.vending.BILLING" />

    <!-- Explicitly remove broad media permissions. We use the Android Photo Picker (no permission needed on Android 13+). -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" tools:node="remove" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" tools:node="remove" />
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" tools:node="remove" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" tools:node="remove" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" tools:node="remove" />

    <!--
      Health Connect: Apollo Reborn does NOT use Health Connect on Android.
      The capacitor-health plugin's manifest declares several READ_* permissions
      that get merged into our APK. Google Play rejected the app for excessive
      Health Connect access, so we explicitly REMOVE the permissions we don't
      use (Heart Rate, Steps, Steps Cadence, Distance, Active Calories, Exercise
      Route). We keep only Exercise (completed workouts) and Total Calories
      Burned, which are the minimum needed for current features.
      iOS Apple HealthKit is unaffected — these tools:node directives only
      apply to the Android manifest merger.
    -->
    <uses-permission android:name="android.permission.health.READ_HEART_RATE" tools:node="remove" />
    <uses-permission android:name="android.permission.health.READ_STEPS" tools:node="remove" />
    <uses-permission android:name="android.permission.health.READ_STEPS_CADENCE" tools:node="remove" />
    <uses-permission android:name="android.permission.health.READ_DISTANCE" tools:node="remove" />
    <uses-permission android:name="android.permission.health.READ_ACTIVE_CALORIES_BURNED" tools:node="remove" />
    <uses-permission android:name="android.permission.health.READ_EXERCISE_ROUTE" tools:node="remove" />
    <!-- Retained (minimum required): -->
    <!-- android.permission.health.READ_EXERCISE -->
    <!-- android.permission.health.READ_TOTAL_CALORIES_BURNED -->
</manifest>
