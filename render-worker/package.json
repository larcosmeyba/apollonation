{
  "name": "apollo-render-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "External FFmpeg render worker for Apollo Reborn — stitches On-Demand class clips into one finished MP4, stores it in Cloudflare R2 (24h lifecycle), and reports back to Supabase.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.658.0",
    "@aws-sdk/s3-request-presigner": "^3.658.0",
    "express": "^4.21.0"
  }
}
